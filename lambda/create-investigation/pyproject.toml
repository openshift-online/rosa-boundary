[project]
name = "create-investigation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyJWT>=2.13.0",
    "cryptography>=50.0.0",
    "requests>=2.34.2",
    "boto3>=1.43.72",
    "awslambdaric==4.0.2",
]

[dependency-groups]
dev = [
    "moto[ecs,efs,iam,kms,s3,sts]>=5.2.2",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-mock>=3.15.1",
]
