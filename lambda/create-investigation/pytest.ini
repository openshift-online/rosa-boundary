[pytest]
# Use importlib import mode to prevent pytest from prepending the lambda directory
# to sys.path. The lambda directory contains bundled Linux x86_64 dependencies
# (built for deployment) that conflict with the macOS-native packages installed
# by uv for local testing.
addopts = --import-mode=importlib
