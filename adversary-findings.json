{
  "scan_date": "2026-06-03",
  "commit": "d974617",
  "findings": [
    {
      "id": "L2",
      "title": "EFS Security Group Allows All Egress",
      "severity": "low",
      "category": "Infrastructure — Overly Permissive Network Configuration",
      "file": "deploy/regional/efs.tf",
      "line": 30,
      "end_line": 36,
      "issue": "The EFS security group allows all outbound traffic to 0.0.0.0/0 on all ports and protocols. EFS mount targets only need inbound NFS (port 2049) and do not initiate outbound connections.",
      "impact": "Minimal direct risk, but defense-in-depth suggests removing unnecessary egress from the EFS security group.",
      "recommendation": "Remove the egress block entirely from the EFS security group. EFS mount targets are passive listeners and require no outbound rules."
    },
    {
      "id": "L3",
      "title": "Example Script Uses assignPublicIp=ENABLED",
      "severity": "low",
      "category": "Infrastructure — Network Exposure",
      "file": "deploy/regional/examples/launch_task.sh",
      "line": 62,
      "end_line": null,
      "issue": "The example launch script uses assignPublicIp=ENABLED when launching Fargate tasks. The production Lambda handler correctly uses DISABLED.",
      "impact": "If an operator copies this example script for production use, Fargate tasks would receive public IP addresses, exposing the SSM agent and container services to the internet.",
      "recommendation": "Change to assignPublicIp=DISABLED in the example script, or add a prominent warning comment."
    },
    {
      "id": "L4",
      "title": "oc_version Not Validated in Lambda Handler",
      "severity": "low",
      "category": "Application — Input Validation",
      "file": "lambda/create-investigation/handler.py",
      "line": 132,
      "end_line": null,
      "issue": "The oc_version parameter from the request body is accepted without validation and passed directly into the task definition environment variables and task tags. The handler validates investigation_id and cluster_id but not oc_version against the set of valid versions {4.14, 4.15, 4.16, 4.17, 4.18, 4.19, 4.20}.",
      "impact": "A malicious or errant client can inject arbitrary strings into the OC_VERSION environment variable and task tags. The entrypoint validates whether the path exists, so invalid versions fall back to default, but the unvalidated tag value pollutes audit tagging.",
      "recommendation": "Add a server-side allowlist check immediately after parsing oc_version:\n\nVALID_OC_VERSIONS = {'4.14', '4.15', '4.16', '4.17', '4.18', '4.19', '4.20'}\nif oc_version not in VALID_OC_VERSIONS:\n    return response(400, {'error': f'Invalid oc_version: must be one of {sorted(VALID_OC_VERSIONS)}'})"
    },
    {
      "id": "L5",
      "title": "Terraform AWS Provider Broadly Pinned and Lockfile Gitignored",
      "severity": "low",
      "category": "Infrastructure — Unpinned Dependency Version",
      "file": "deploy/regional/main.tf",
      "line": 7,
      "end_line": null,
      "issue": "The AWS provider is pinned with version = \"~> 5.0\", which allows any 5.x release. The .terraform.lock.hcl is explicitly gitignored in both deploy/regional/.gitignore and the root .gitignore, meaning different Terraform runs can use different provider versions with no lock enforcement.",
      "impact": "A compromised or buggy provider release within the 5.x range could be silently adopted on the next terraform init. Without a committed lockfile, supply chain integrity of the provider binary is not verifiable in CI.",
      "recommendation": "Pin to a narrower range (e.g., \"~> 5.80\") and remove .terraform.lock.hcl from .gitignore so it is committed and verified in CI."
    },
    {
      "id": "L6",
      "title": "Unpinned Fedora Base Image",
      "severity": "low",
      "category": "Infrastructure — Unpinned Image Tag",
      "file": "Containerfile",
      "line": 3,
      "end_line": null,
      "issue": "The base image FROM fedora:43 is pinned to a major version tag but not to an immutable digest. The fedora:43 tag can be updated in-place when security patches are released.",
      "impact": "Rebuilds could silently pull a different base image with different packages. For a security-sensitive SRE tooling container used in production, reproducible builds matter.",
      "recommendation": "Pin to a digest:\n\nFROM fedora:43@sha256:<digest>"
    },
    {
      "id": "L7",
      "title": "Claude Code Installed via Piped Shell Script Without Integrity Verification",
      "severity": "low",
      "category": "Application — Supply Chain",
      "file": "Containerfile",
      "line": 72,
      "end_line": null,
      "issue": "Claude Code is installed by piping a remote script to bash: curl -fsSL https://claude.ai/install.sh | INSTALL_DIR=/usr/local/lib/claude-code BIN_DIR=/usr/local/bin bash. No checksum or signature verification is performed on the installer.",
      "impact": "A compromised CDN or MITM at build time could inject malicious code into the container image. The -fsSL flags use HTTPS which reduces MITM risk, but there is no integrity gate.",
      "recommendation": "If Claude Code publishes checksums or release signatures, verify them after download. Alternatively, pre-stage the installer in a trusted internal registry and pull from there."
    },
    {
      "id": "M1",
      "title": "Lambda ECS IAM Policy Uses Wildcard Resource",
      "severity": "medium",
      "category": "Infrastructure — Overly Permissive IAM Policy",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 44,
      "end_line": 56,
      "issue": "The Lambda IAM policy grants ecs:RunTask, ecs:StopTask, ecs:ListTasks, ecs:DescribeTasks, ecs:DescribeTaskDefinition, ecs:RegisterTaskDefinition, ecs:DeregisterTaskDefinition, and ecs:TagResource on Resource = \"*\". This allows the Lambda function to manage tasks and task definitions across any ECS cluster in the account.",
      "impact": "If the Lambda is compromised via a token validation bypass or dependency supply chain attack, the attacker can launch, stop, or register task definitions in any cluster in the AWS account.",
      "recommendation": "Scope ECS resources to the specific cluster. Note that RegisterTaskDefinition and DeregisterTaskDefinition do not support resource-level IAM scoping and must retain *, but RunTask, StopTask, ListTasks, DescribeTasks, and TagResource can be scoped:\n\nResource = [\n  aws_ecs_cluster.main.arn,\n  \"arn:${data.aws_partition.current.partition}:ecs:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:task/${aws_ecs_cluster.main.name}/*\",\n  \"arn:${data.aws_partition.current.partition}:ecs:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:task-definition/${var.project}-${var.stage}*\"\n]"
    },
    {
      "id": "M2",
      "title": "Shared SRE Role Grants KMS Decrypt on All Keys",
      "severity": "medium",
      "category": "Infrastructure — Overly Permissive IAM Policy",
      "file": "deploy/regional/oidc.tf",
      "line": 232,
      "end_line": 239,
      "issue": "The KMSForECSExec statement in the shared SRE role's ABAC policy grants kms:Decrypt and kms:GenerateDataKey on Resource = \"*\". Any SRE who assumes this role can use any KMS key in the account, subject only to key policies.",
      "impact": "An SRE could decrypt data encrypted with any CMK that has a permissive key policy. The task role (iam.tf) correctly scopes to aws_kms_key.exec_session.arn, making this gap in the shared SRE role an inconsistency.",
      "recommendation": "Scope the KMS resource to the exec session key:\n\nResource = aws_kms_key.exec_session.arn"
    },
    {
      "id": "M3",
      "title": "CloudWatch Logs Encryption Disabled for ECS Exec Sessions",
      "severity": "medium",
      "category": "Infrastructure — Encryption Gap",
      "file": "deploy/regional/ecs.tf",
      "line": 17,
      "end_line": null,
      "issue": "The ECS cluster execute command configuration sets cloud_watch_encryption_enabled = false. While a KMS key is referenced for session-level encryption (kms_key_id = aws_kms_key.exec_session.arn), the CloudWatch log group storing SSM session logs does not use CMK encryption.",
      "impact": "SSM session logs may contain sensitive commands and output from SRE investigations. Without CMK encryption on the log group, there is no independent audit trail for key usage and no ability to revoke access to historical logs via key policy.",
      "recommendation": "Set cloud_watch_encryption_enabled = true and add kms_key_id to the ssm_sessions log group resource:\n\nresource \"aws_cloudwatch_log_group\" \"ssm_sessions\" {\n  name              = \"/ecs/${var.project}-${var.stage}/ssm-sessions\"\n  retention_in_days = var.retention_days\n  kms_key_id        = aws_kms_key.exec_session.arn\n}"
    },
    {
      "id": "M4",
      "title": "S3 Audit Bucket Uses SSE-S3 Instead of SSE-KMS",
      "severity": "medium",
      "category": "Infrastructure — Encryption Configuration",
      "file": "deploy/regional/s3.tf",
      "line": 47,
      "end_line": 51,
      "issue": "The S3 audit bucket uses sse_algorithm = \"AES256\" (SSE-S3) instead of aws:kms (SSE-KMS). SSE-S3 uses AWS-managed keys with no audit trail for key usage and no ability to control access via KMS key policies.",
      "impact": "For an audit log bucket with WORM compliance (Object Lock), the inability to independently audit encryption key usage or enforce key-level access controls weakens the security posture. Any principal with s3:GetObject permission can decrypt objects; there is no separate kms:Decrypt gate.",
      "recommendation": "Use KMS encryption with a dedicated CMK:\n\napply_server_side_encryption_by_default {\n  sse_algorithm     = \"aws:kms\"\n  kms_master_key_id = aws_kms_key.audit_bucket.arn\n}"
    },
    {
      "id": "M5",
      "title": "No EFS Filesystem Policy Enforcing IAM Authorization",
      "severity": "medium",
      "category": "Infrastructure — Missing Access Control",
      "file": "deploy/regional/efs.tf",
      "line": 1,
      "end_line": null,
      "issue": "The EFS filesystem has no aws_efs_file_system_policy resource. While the access points require IAM (iam = \"ENABLED\" in the task definition), the filesystem itself does not enforce IAM authorization or deny anonymous access. Any entity with network access to the mount targets (port 2049) could potentially mount the filesystem directly without an access point.",
      "impact": "If the Fargate security group or VPC network controls are misconfigured, an attacker with network access to the NFS port could mount the EFS filesystem as root and access all investigation data across all access points.",
      "recommendation": "Add an aws_efs_file_system_policy that enforces IAM authentication and requires access via access points:\n\nresource \"aws_efs_file_system_policy\" \"sre_home\" {\n  file_system_id = aws_efs_file_system.sre_home.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [{\n      Effect    = \"Deny\"\n      Principal = { AWS = \"*\" }\n      Action    = [\"elasticfilesystem:ClientMount\", \"elasticfilesystem:ClientWrite\"]\n      Resource  = aws_efs_file_system.sre_home.arn\n      Condition = {\n        Bool = { \"elasticfilesystem:AccessedViaMountTarget\" = \"false\" }\n      }\n    }]\n  })\n}"
    },
    {
      "id": "M6",
      "title": "S3 Audit Bucket Missing Deny-Non-TLS Policy and Access Logging",
      "severity": "medium",
      "category": "Infrastructure — Missing Access Control and Auditability",
      "file": "deploy/regional/s3.tf",
      "line": 1,
      "end_line": null,
      "issue": "The S3 audit bucket has no aws_s3_bucket_policy to enforce encryption in transit (deny non-TLS requests). It also has no S3 server access logging configured for the bucket itself.",
      "impact": "Without a deny-non-TLS policy, misconfigured clients or proxies could theoretically write over unencrypted HTTP. Without server access logging, unauthorized access attempts to audit data would not be independently recorded.",
      "recommendation": "Add a bucket policy requiring TLS:\n\nresource \"aws_s3_bucket_policy\" \"audit\" {\n  bucket = aws_s3_bucket.audit.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [{\n      Sid       = \"DenyNonTLS\"\n      Effect    = \"Deny\"\n      Principal = \"*\"\n      Action    = \"s3:*\"\n      Resource  = [aws_s3_bucket.audit.arn, \"${aws_s3_bucket.audit.arn}/*\"]\n      Condition = { Bool = { \"aws:SecureTransport\" = \"false\" } }\n    }]\n  })\n}"
    },
    {
      "id": "M7",
      "title": "Lambda CORS Allows All Origins",
      "severity": "medium",
      "category": "Application — Overly Permissive CORS",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 167,
      "end_line": null,
      "issue": "The Lambda function URL CORS configuration sets allow_origins = [\"*\"] with a comment 'Allow localhost for testing; restrict in production' — but this is not stage-gated and applies to all environments.",
      "impact": "A malicious website could make cross-origin requests to the Lambda function URL. The primary auth mechanism is SigV4 (which cannot be replicated cross-origin), but the wildcard CORS combined with Access-Control-Allow-Headers: x-oidc-token means any page with a valid OIDC token could invoke the function.",
      "recommendation": "Restrict CORS origins to known SRE tool domains or remove CORS entirely since the CLI uses direct SDK invocation rather than browser-based HTTP."
    },
    {
      "id": "M7",
      "title": "Lambda CORS Allows All Origins",
      "severity": "medium",
      "category": "Application — Overly Permissive CORS",
      "file": "lambda/create-investigation/handler.py",
      "line": 803,
      "end_line": null,
      "issue": "The Lambda handler response function sets Access-Control-Allow-Origin: * on every response, including 200 success responses containing task ARNs and role ARNs.",
      "impact": "Sensitive response fields (task_arn, role_arn, access_point_id) are returned with a wildcard CORS header, allowing any cross-origin script that obtains a valid OIDC token to read these values.",
      "recommendation": "Match the CORS origin header to a configured allowlist rather than reflecting *. If the function is only called via CLI SDK invocation, remove the CORS headers entirely."
    },
    {
      "id": "M8",
      "title": "Container Grants Passwordless Sudo ALL",
      "severity": "medium",
      "category": "Application — Container Privilege Escalation",
      "file": "Containerfile",
      "line": 66,
      "end_line": null,
      "issue": "The SRE user is granted NOPASSWD: ALL sudo access: echo 'sre ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/sre. This allows any user who connects via ECS Exec to escalate to root inside the container.",
      "impact": "A compromised or malicious SRE session can escalate to root inside the container. Root access enables modification of entrypoint/cleanup scripts (bypassing S3 audit sync), installing additional tools, or attempting to use the task role IAM credentials for lateral movement. The entrypoint already runs as root and handles alternatives switching, so this breadth of sudo is unnecessary.",
      "recommendation": "Restrict sudo to the minimum commands the sre user requires (e.g., alternatives only):\n\nsre ALL=(ALL) NOPASSWD: /usr/sbin/alternatives\n\nIf the entrypoint handles all root operations at startup, the sre user may need no sudo at all."
    },
    {
      "id": "M9",
      "title": "Python Lambda Dependencies Use >= Version Pinning in pyproject.toml",
      "severity": "medium",
      "category": "Application — Unpinned Dependency Version",
      "file": "lambda/create-investigation/pyproject.toml",
      "line": 6,
      "end_line": 9,
      "issue": "Lambda dependencies use >= specifiers in pyproject.toml: PyJWT>=2.8.0, cryptography>=41.0.0, requests>=2.31.0, boto3>=1.34.0. A uv.lock file exists and is committed, which does pin the actual resolved versions used by the build toolchain. However, the pyproject.toml itself does not reflect exact pinned versions, so alternative install paths (pip install -r, direct pip install .) would pull current-latest rather than the locked versions.",
      "impact": "If the Lambda is built via a path that bypasses uv.lock (e.g., pip install from pyproject.toml directly), a compromised or buggy version of PyJWT, cryptography, or requests could be silently installed. The cryptography and PyJWT packages are high-value targets for supply chain attacks.",
      "recommendation": "Change pyproject.toml to use == pinning matching the uv.lock resolved versions, and ensure the Lambda build Makefile always uses uv sync --frozen to enforce the lockfile."
    },
    {
      "id": "M10",
      "title": "Bedrock IAM Policy Allows All Regions and All Models",
      "severity": "medium",
      "category": "Infrastructure — Overly Permissive IAM Policy",
      "file": "deploy/regional/iam.tf",
      "line": 157,
      "end_line": 163,
      "issue": "The Bedrock policy on the task role grants bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream on arn:${partition}:bedrock:*:*:inference-profile/* and arn:${partition}:bedrock:*:*:foundation-model/*. The wildcard regions and all-model access mean the task role can invoke any foundation model in any region.",
      "impact": "If an SRE container is compromised, the attacker can invoke arbitrary Bedrock models in any region, leading to cost abuse. Data sent to Bedrock models outside the expected region may also violate data residency requirements.",
      "recommendation": "Scope to the specific region and model family:\n\nResource = [\n  \"arn:${data.aws_partition.current.partition}:bedrock:${data.aws_region.current.name}::foundation-model/anthropic.claude-*\",\n  \"arn:${data.aws_partition.current.partition}:bedrock:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:inference-profile/*\"\n]"
    },
    {
      "id": "M11",
      "title": "Lambda IAM Policy Missing elasticfilesystem:DeleteAccessPoint Permission Causes Silent Cleanup Failures",
      "severity": "medium",
      "category": "Infrastructure — IAM Policy Mismatch",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 70,
      "end_line": 88,
      "issue": "The Lambda's EFS IAM policy grants elasticfilesystem:CreateAccessPoint, elasticfilesystem:DescribeAccessPoints, and elasticfilesystem:TagResource, but does NOT grant elasticfilesystem:DeleteAccessPoint. The handler calls efs.delete_access_point() in four cleanup paths (lines 671, 727, 756, 782 of handler.py) — these calls will receive AccessDenied errors silently swallowed by bare except: pass blocks.",
      "impact": "When task launch fails after EFS access point creation, the rollback silently fails, leaving orphaned EFS access points. An attacker who can repeatedly trigger task launch failures (e.g., by exhausting capacity or injecting bad task definitions) could accumulate access points approaching the 10,000-per-filesystem limit, causing a denial-of-service for all investigations.",
      "recommendation": "Add elasticfilesystem:DeleteAccessPoint to the Lambda EFS IAM policy, scoped to the specific filesystem ARN:\n\nAction = [\n  \"elasticfilesystem:CreateAccessPoint\",\n  \"elasticfilesystem:DescribeAccessPoints\",\n  \"elasticfilesystem:DeleteAccessPoint\",\n  \"elasticfilesystem:TagResource\"\n]\nResource = aws_efs_file_system.sre_home.arn"
    },
    {
      "id": "M12",
      "title": "kube-proxy Sidecar Writes Kubeconfig to Shared Ephemeral Volume Readable by SRE User",
      "severity": "medium",
      "category": "Application — Credential Exposure",
      "file": "deploy/regional/ecs.tf",
      "line": 148,
      "end_line": 152,
      "issue": "When the kube-proxy sidecar is enabled, its startup command writes the cluster kubeconfig to /tmp/kubeconfig inside the proxy-tmp ephemeral volume: printf '%s' \"$KUBECONFIG_DATA\" > /tmp/kubeconfig. The rosa-boundary container's entrypoint also writes a kubeconfig pointing to localhost:${KUBE_PROXY_PORT} at /home/sre/.kube/config. The secret is injected at container launch via ECS Secrets Manager injection (KUBECONFIG_DATA), but then stored as a plaintext file accessible within the container.",
      "impact": "The sre user (and any process running as sre) can read the kubeconfig from /home/sre/.kube/config, which grants API access to the ROSA cluster via the proxy. More critically, the kubeconfig in /tmp/kubeconfig within the proxy-tmp volume is writable by the kube-proxy process. If the proxy container is compromised, an attacker could overwrite the kubeconfig to redirect API calls.",
      "recommendation": "Use readOnlyRootFilesystem on the kube-proxy sidecar (already set) but additionally: (1) Set the kubeconfig file to read-only after creation (chmod 400), (2) Use a named pipe or env-var injection instead of a file where possible, (3) Restrict the S3 sync in entrypoint.sh from syncing .kube/ contents to audit storage."
    },
    {
      "id": "H1",
      "title": "Lambda Token Validation Accepts Tokens from Any of Three Issuers Without Centralized Rate Limiting or Replay Protection",
      "severity": "high",
      "category": "Application — Authentication",
      "file": "lambda/create-investigation/handler.py",
      "line": 345,
      "end_line": 388,
      "issue": "The validate_oidc_token function now supports three OIDC issuers (primary Keycloak, stage EmployeeIDP, production EmployeeIDP). Issuer routing is based on the unverified 'iss' claim from the token — validated before signature check but used to select which JWKS endpoint to validate against. There is no replay protection: a valid token can be submitted multiple times within its expiry window to create multiple investigations. There is also no rate limiting on the Lambda function URL itself.",
      "impact": "An attacker who obtains a valid short-lived OIDC token (e.g., from a phished SRE, log leak, or intercepted request) can replay it to create multiple ECS tasks and EFS access points until the token expires, potentially exhausting EFS access point quotas or running up ECS costs. With three supported issuers, compromising any one IdP's signing key would allow forging tokens accepted by the Lambda.",
      "recommendation": "1. Implement token replay protection using a DynamoDB or ElastiCache nonce store keyed on the token jti claim. 2. Add Lambda concurrency throttling and consider AWS WAF rate limiting on the function URL. 3. Regularly rotate JWKS and monitor for unexpected issuer configurations."
    }
  ]
}
